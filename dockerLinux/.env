# Docker project name - CHANGE THIS for every new project (see dockerLinux/README.md).
# Lowercase letters, digits, - and _ only. Names the containers (<name>-web-1, <name>-db-1),
# the web image (<name>_web81:local) and the database volume (<name>_db_data).
PROJECT_NAME=setupcase-core
